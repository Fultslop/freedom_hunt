{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Splash Screen",
  "type": "object",
  "additionalProperties": false,
  "required": ["template-type", "image", "title"],
  "properties": {
    "template-type": { "const": "splash" },
    "image": { "type": "string" },
    "shader": { "enum": ["none", "grayscale", "duotone", "vignette", "darken"] },
    "effect": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": { "enum": ["confetti", "shooting-stars", "fireworks"] },
        "cooldown": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "min": { "type": "number" },
            "max": { "type": "number" }
          }
        },
        "max": { "type": "number" }
      }
    },
    "title": { "type": "string" },
    "anchor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["horizontal", "vertical"],
      "properties": {
        "horizontal": { "enum": ["left", "center", "right"] },
        "vertical": { "enum": ["top", "center", "bottom"] }
      }
    },
    "nav-bar": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "visible": { "type": "boolean" } }
    }
  }
}
